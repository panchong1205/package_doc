import { defineConfig } from 'dumi';

export default defineConfig({
  outputPath: 'docs-dist',
  publicPath: 'https://panchong1205.github.io/panchong.github.io/docs-dist/',
  themeConfig: {
    name: 'pc_packages',
  },
});
